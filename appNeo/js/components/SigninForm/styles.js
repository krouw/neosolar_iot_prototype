import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    signigForm: {
      width: '100%',
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
    },
    signinError: {
      padding: 16,
    },
    test :{
     borderStyle: 'solid',
     borderColor: 'red',
     borderWidth: 1,
    },
});
